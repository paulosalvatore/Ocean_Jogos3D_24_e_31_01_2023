using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cannon : MonoBehaviour
{
    /*
    1. A cada X segundos, executamos um método que cria um projétil novo
    2. Assim que o projétil for criado, ativamos o GameObject para que o movimento seja iniciado
    */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
